const express = require("express");
const nanoid = require("nano-id")
const { ShortUrlModel } = require("express");

const urlRouter = express.Router();

console.log(2+4)
module.exports = { urlRouter };
